const profileModel = require('../models/profileSchema');

module.exports = {
    name: 'search',
    cooldown: 60000,
    description: "Search for some money around certain locations",
    async execute(message, args, cmd, client, Discord, profileData) {

    }
}